import { base } from '$app/paths';
export function getPathFromBase(path) {
    if (path === '/')
        return base || '/';
    if (!base || !path.startsWith('/') || (path === base || path.startsWith("".concat(base, "/"))))
        return path;
    return "".concat(base).concat(path);
}
export function parseImageSrc(src) {
    if (src.startsWith('//'))
        return src;
    return getPathFromBase(src);
}
export function isLinkActive(link, routeId) {
    // Normalize both by removing trailing slashes for comparison
    var normalizedLink = (link === null || link === void 0 ? void 0 : link.replace(/\/$/, '')) || '';
    var normalizedRouteId = (routeId === null || routeId === void 0 ? void 0 : routeId.replace(/\/$/, '')) || '';
    return normalizedLink === normalizedRouteId || (link === null || link === void 0 ? void 0 : link.startsWith("".concat(normalizedRouteId, "/")));
}
