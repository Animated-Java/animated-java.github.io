import"../chunks/DsnmJJEf.js";import"../chunks/NxOm-O3r.js";import{t,u as n,v as o}from"../chunks/C3L1cWEi.js";var s=n(`<h1 id="cameras">Cameras</h1> <p>Cameras are positional anchors that track a specific point in space as your rig animates. They are useful for attaching player view effects, executing commands at specific view positions, or tracking a point of interest relative to the rig.</p> <h2 id="installing-the-camera-plugin">Installing the Camera Plugin</h2> <p>Animated Java does not include camera nodes by default. To use cameras, you must install the <a href="https://www.blockbench.net/plugins/cameras" rel="nofollow">Camera Plugin</a> from the Blockbench plugin library.</p> <h2 id="entity-creation">Entity Creation</h2> <p>A Camera always creates a <code>minecraft:item_display</code> entity when exported. Unlike bone entities, cameras are <strong>not passengers</strong> of the root entity — they are free-floating entities that are repositioned every tick by the rig’s tick function using smooth teleportation (<code>teleport_duration: 2</code>).</p> <p>Their position and rotation is stored as <code>&#123;px, py, pz, rx, ry&#125;</code> in the rig’s data storage entry under <code>entry.data.cameras.&lt;name&gt;</code>, and updated each tick from the current animation frame.</p> <h2 id="what-cameras-are-good-for">What Cameras Are Good For</h2> <ul><li><strong>Spectator-mode view anchors</strong> — Teleport a spectator player to the camera entity each tick to get a first-person view from within the rig.</li> <li><strong>Positional effects</strong> — Spawn particles, play sounds, or run commands at an animated position that isn’t tied to a physical bone.</li> <li><strong>Animation-driven targeting</strong> — Point the camera at something of interest and have the engine track it automatically as the animation plays.</li></ul> <h2 id="camera-config">Camera Config</h2> <p>Cameras have no additional configuration dialog beyond what is set in Blockbench. Position, rotation, and animation are controlled in the standard Blockbench viewport and timeline.</p> <h2 id="accessing-camera-entities">Accessing Camera Entities</h2> <p>Use the <code>as_camera</code> <a href="/docs/function-api/utilities#camera-functions">utility function</a>:</p> <pre class="language-mcfunction"><code><span class="token comment">#ARGS: &#123;name: string, command: string&#125;</span>
<span class="token keyword">execute</span> <span class="token keyword">as</span> <span class="token selector class-name">@e</span><span class="token punctuation">[</span><span class="token attr-name property">tag</span><span class="token operator">=</span><span class="token attr-name property">my_pack.my_rig.root</span><span class="token punctuation">]</span> <span class="token attr-name property">run</span> <span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/as_camera</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">name</span><span class="token operator">:</span> <span class="token string">"eye"</span><span class="token operator">,</span> <span class="token attr-name property">command</span><span class="token operator">:</span> <span class="token string">"ride @s mount @p"</span><span class="token punctuation">&#125;</span></code></pre> <h2 id="notes">Notes</h2> <ul><li>Cameras do not render any visible geometry in Minecraft — the <code>item_display</code> entity they create is invisible. They are purely positional anchors.</li> <li>Only position and two-axis rotation (<code>rx</code>, <code>ry</code>) are stored per-frame. Camera roll is not supported.</li></ul>`,1);function p(a){var e=s();o(30),t(a,e)}export{p as component};
