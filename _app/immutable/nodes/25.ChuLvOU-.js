import"../chunks/DsnmJJEf.js";import"../chunks/DloFAEu7.js";import{n as i,o,q as n}from"../chunks/BTBjDpav.js";var a=o('<h1 id="groups">Groups</h1> <p>Groups (also called <strong>bones</strong>) are used to create hierarchies and to apply transformations to multiple nodes at once. They are the primary tool for rigging and animating a model in Animated Java.</p> <h2 id="entity-creation">Entity Creation</h2> <p>A Group only creates a <code>minecraft:item_display</code> entity when exported if it has <a href="/docs/nodes/cubes">Cube</a> children. Otherwise, it is only used for organizational and rigging purposes in the editor.</p> <aside class="admonition admonition-tip"><p class="admonition-title">Tip</p><div class="admonition-content"><p>Groups without Cube children are called <strong>Structure Groups</strong>, and have a unique icon in the editor to distinguish them from regular Groups. Structure Groups are free — they don’t create an entity and cost nothing at runtime.</p></div></aside> <h2 id="display-entity-config">Display Entity Config</h2> <p>Because Groups that contain Cubes create a <code>minecraft:item_display</code> entity, they expose the full <strong>Display Entity Config</strong>. Right-click on the group in the outliner and select <strong>Display Entity Config</strong> to open it.</p> <p>The config has a <strong>global</strong> section (applies regardless of variant) and a <strong>per-variant</strong> section (can be different for each variant).</p> <h3 id="global-options">Global Options</h3> <ul><li><h4 id="on-summon-function">On-Summon Function</h4> <p>Commands executed <code>as</code> and <code>at</code> this entity when it is summoned.</p> <p>All functions support <a href="https://mcbuild.dev" rel="nofollow">MC-Build</a> syntax.</p></li></ul> <h3 id="per-variant-options">Per-Variant Options</h3> <p>These options can be configured independently for each variant using the <strong>Variant</strong> selector at the top of the config dialog.</p> <ul><li><h4 id="on-apply-function">On-Apply Function</h4> <p>Commands executed <code>as</code> this entity when the selected variant is applied to the rig.</p></li> <li><h4 id="billboard">Billboard</h4> <p>Controls whether the bone entity pivots to face the player.</p> <ul><li><code>fixed</code> — No billboarding. The entity faces its rigged direction.</li> <li><code>horizontal</code> — Pivots around the vertical axis to face the player.</li> <li><code>vertical</code> — Pivots around the horizontal axis to face the player.</li> <li><code>center</code> — Always faces the player around both axes.</li></ul></li> <li><h4 id="override-brightness">Override Brightness</h4> <p>When enabled, the entity ignores ambient lighting and uses the specified sky and block brightness values instead.</p> <ul><li><strong>Sky Brightness</strong> — Sky light level (0–15).</li> <li><strong>Block Brightness</strong> — Block light level (0–15).</li></ul></li> <li><h4 id="enchanted">Enchanted</h4> <p>Adds the enchantment glint effect to the bone’s item model. Useful for making a glowing sword, magic item, or highlight effect.</p></li> <li><h4 id="glowing">Glowing</h4> <p>Makes the entity emit a glow outline (as if it has the glowing status effect).</p> <ul><li><strong>Override Glow Color</strong> — When enabled, uses a custom color for the glow outline.</li> <li><strong>Glow Color</strong> — The hex color for the glow outline.</li></ul></li> <li><h4 id="shadow-radius">Shadow Radius</h4> <p>The radius of the entity’s circular shadow, in blocks. Set to <code>0</code> to disable the shadow.</p></li> <li><h4 id="shadow-strength">Shadow Strength</h4> <p>The opacity of the shadow (0.0 – 1.0).</p></li></ul>',1);function d(e){var t=a();n(24),i(e,t)}export{d as component};
