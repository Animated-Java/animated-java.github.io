import"../chunks/DsnmJJEf.js";import"../chunks/DloFAEu7.js";import{n as i,o,q as n}from"../chunks/BTBjDpav.js";var a=o('<h1 id="blueprints">Blueprints</h1> <h2 id="what-is-a-blueprint">What is a Blueprint?</h2> <p>A <strong>Blueprint</strong> is Animated Java’s custom Blockbench project format (<code>.ajblueprint</code>). It stores everything needed to generate a working Minecraft rig: your 3D model, textures, animations, variants, and all export configuration.</p> <p>When you export a Blueprint, Animated Java compiles it into a <strong>Resource Pack</strong> (containing your custom models and textures) and a <strong>Data Pack</strong> (containing all the MCfunction logic to summon, animate, and control your rig in-game).</p> <hr/> <h2 id="blueprint-settings">Blueprint Settings</h2> <p>Blueprint Settings can be opened from <strong>Animated Java <i class="minecraft-right-arrow"></i> Blueprint Settings</strong> in the Blockbench menu bar, or from the Animated Java panel.</p> <h3 id="general">General</h3> <ul><li><h4 id="blueprint-id">Blueprint ID</h4> <p>A namespaced identifier (e.g. <code>my_pack:my_rig</code>) that uniquely identifies this blueprint. This ID is used as the namespace and path for every generated function, function tag, storage key, and model path.</p> <aside class="admonition admonition-info"><p class="admonition-title">Example</p><div class="admonition-content"><p><code>my_pack:my_rig</code> generates functions at <code>data/my_pack/function/my_rig/...</code></p></div></aside> <aside class="admonition admonition-tip"><p class="admonition-title">Tip</p><div class="admonition-content"><p>Choose a unique ID. If two blueprints share the same ID, their exported files will overwrite each other.</p></div></aside> <aside class="admonition admonition-danger"><p class="admonition-title">Restrictions</p><div class="admonition-content"><p></p><ul><li><p>Cannot use the <code>minecraft:</code> namespace.</p></li> <li><p>Cannot use <code>animated_java:global</code>.</p></li> <li><p>May only contain lowercase letters, digits, underscores, forward slashes, and a single colon.</p></li></ul></div></aside></li> <li><h4 id="target-minecraft-version">Target Minecraft Version</h4> <p>The Minecraft version to compile for. Animated Java uses this to pick the correct pack format, item component format, NBT structure, and enchantment syntax.</p> <p><strong>Minimum supported version:</strong> 1.20.4</p> <p>Changing this value after you have already exported may require re-exporting to update generated files.</p></li> <li><h4 id="enable-plugin-mode">Enable Plugin Mode</h4> <p>Switches the export pipeline to produce a single <code>.json</code> file instead of a data pack. This file is consumed by a Minecraft server plugin rather than a vanilla data pack. When plugin mode is active, the Data Pack settings are replaced by Plugin settings.</p></li></ul> <h3 id="resource-pack">Resource Pack</h3> <ul><li><h4 id="resource-pack-export-mode">Resource Pack Export Mode</h4> <p>Controls how the resource pack output is written.</p> <ul><li><strong><code>folder</code></strong> — Merges generated files directly into a resource pack directory on disk. Ideal for live development.</li> <li><strong><code>zip</code></strong> — Writes all generated files into a <code>.zip</code> archive.</li> <li><strong><code>none</code></strong> — Skips resource pack output entirely. Only valid when your blueprint contains no custom models (e.g. you are only using vanilla Block Display or Item Display nodes).</li></ul></li> <li><h4 id="resource-pack-path">Resource Pack Path</h4> <p>The path to the target resource pack folder or zip file. Must point to a valid resource pack (a directory containing <code>pack.mcmeta</code>).</p></li> <li><h4 id="display-item">Display Item</h4> <p>The Minecraft item whose model is overridden to render rig bones. Animated Java assigns each bone a unique <code>CustomModelData</code> value on this item.</p> <aside class="admonition admonition-note"><p class="admonition-title">Note</p><div class="admonition-content"><p>This setting is only relevant for <strong>Minecraft 1.20.4 – 1.21.1</strong>. In 1.21.2 and later, Animated Java uses the <code>minecraft:item_model</code> item component, so no display item override is needed.</p></div></aside> <p>The item must be a <code>generated</code>-type item (e.g. <code>minecraft:white_dye</code>, <code>minecraft:stick</code>). Avoid items with special rendering such as tools, armor, or blocks.</p></li> <li><h4 id="custom-model-data-offset">Custom Model Data Offset</h4> <p>An integer offset added to all assigned CustomModelData values. Useful for avoiding conflicts with other resource packs that also use CustomModelData on the same display item.</p> <aside class="admonition admonition-note"><p class="admonition-title">Note</p><div class="admonition-content"><p>Only relevant for Minecraft 1.20.4 – 1.21.1.</p></div></aside></li></ul> <h3 id="data-pack">Data Pack</h3> <ul><li><h4 id="data-pack-export-mode">Data Pack Export Mode</h4> <p>Controls how the data pack output is written. Same options as <a href="#resource-pack-export-mode">Resource Pack Export Mode</a>: <code>folder</code>, <code>zip</code>, or <code>none</code>.</p></li> <li><h4 id="data-pack-path">Data Pack Path</h4> <p>The path to the target data pack folder or zip file.</p></li> <li><h4 id="auto-update-rig-orientation">Auto Update Rig Orientation</h4> <p>When enabled, Animated Java automatically updates the rig root entity’s facing direction each tick to match its movement direction. Disable this if you want to control orientation manually.</p></li> <li><h4 id="use-storage-for-animation">Use Storage for Animation</h4> <p>When enabled, animation frame data is written to command storage. This significantly reduces the number of generated function files, at the cost of 40% slower animation performance.</p> <p>When disabled (the default), each animation frame is a separate MCfunction file. This produces more files but is much faster to run in-game.</p></li></ul> <h3 id="rig">Rig</h3> <ul><li><h4 id="interpolation-duration">Interpolation Duration</h4> <p>The number of ticks all bone display entities use for client-side transform interpolation (MC’s <code>interpolation_duration</code> NBT field). Higher values produce smoother but more delayed-looking motion. The default is <code>1</code>.</p></li> <li><h4 id="teleportation-duration">Teleportation Duration</h4> <p>Interpolation duration in ticks for teleportation. Values are clamped between 0 and 59 (inclusive). The default is <code>1</code>.</p></li> <li><h4 id="custom-rig-entity-tags">Custom Rig Entity Tags</h4> <p>A comma-separated list of extra scoreboard tags added to all entities in the rig when it is summoned. Useful for identifying your rig entities in custom commands.</p> <aside class="admonition admonition-note"><p class="admonition-title">Example</p><div class="admonition-content"><p><code>my_pack.my_rig.special, summit.booth_entity</code></p></div></aside></li> <li><h4 id="use-entity-stacking">Use Entity Stacking</h4> <p>When enabled (the default), all bone display entities ride the root entity as passengers. This is the standard and most performant approach.</p> <p>When disabled, bone entities are repositioned via teleportation every tick instead. This is <em>slightly</em> less performant, however, it provides <strong><em>much</em></strong> smoother rotations.</p></li></ul> <h3 id="event-functions">Event Functions</h3> <p>Event functions are snippets of MCfunction commands injected into specific points in the rig’s lifecycle. All functions support <a href="https://mcbuild.dev" rel="nofollow">MC-Build</a> syntax and are executed <code>as</code> and <code>at</code> the root entity unless noted.</p> <ul><li><h4 id="on-summon-function">On-Summon Function</h4> <p>Commands injected into the <code>summon</code> function, executed once when the rig is first spawned.</p></li> <li><h4 id="on-remove-function">On-Remove Function</h4> <p>Commands injected into the <code>remove/this</code> function, executed once when the rig is removed.</p></li> <li><h4 id="on-pre-tick-function">On-Pre-Tick Function</h4> <p>Commands injected at the <strong>beginning</strong> of the <code>tick</code> function, executed every game tick before bone transforms are applied.</p></li> <li><h4 id="on-post-tick-function">On-Post-Tick Function</h4> <p>Commands injected at the <strong>end</strong> of the <code>tick</code> function, executed every game tick after bone transforms are applied.</p></li></ul> <h3 id="render-box">Render Box</h3> <p>The render box controls the culling volume that Minecraft uses to decide whether to render the rig. If the root entity’s bounding box leaves the player’s view frustum, the entire rig (including all passengers) is hidden.</p> <ul><li><h4 id="show-render-box">Show Render Box</h4> <p>Toggles a wireframe preview of the render box in the Blockbench viewport.</p></li> <li><h4 id="auto-render-box">Auto Render Box</h4> <p>When enabled, Animated Java automatically calculates a render box large enough to contain all bones across all animation frames, with an 8-unit overflow margin. <strong>Recommended to leave this on.</strong></p></li> <li><h4 id="render-box-size">Render Box Size</h4> <p>Manual render box dimensions in blocks (half-width and height). Only active when <strong>Auto Render Box</strong> is disabled.</p> <aside class="admonition admonition-warning"><p class="admonition-title">Warning</p><div class="admonition-content"><p>Setting the render box too small will cause the rig to disappear (cull) when partially off-screen. Setting it too large wastes rendering resources.</p></div></aside></li></ul> <h3 id="plugin-settings">Plugin Settings</h3> <p>Only visible when <a href="#enable-plugin-mode">Enable Plugin Mode</a> is turned on.</p> <ul><li><h4 id="json-file-path">JSON File Path</h4> <p>The path to write the exported plugin blueprint JSON file.</p></li> <li><h4 id="baked-animations">Baked Animations</h4> <p>When enabled, animations are fully pre-computed (baked) and embedded in the JSON output. When disabled, raw keyframe data is embedded instead and the server plugin performs interpolation itself.</p></li></ul> <hr/> <h2 id="exporting-your-blueprint">Exporting Your Blueprint</h2> <p>Once your blueprint is configured, click <strong>Animated Java > Export (Debug)</strong> to generate the resource pack and data pack files.</p> <p>Animated Java tracks every file it writes using an <code>.ajmeta</code> file stored next to the pack. On the next export, stale files from the previous export are automatically cleaned up.</p> <aside class="admonition admonition-tip"><p class="admonition-title">Tip</p><div class="admonition-content"><p>After exporting, run <code>/reload</code> in Minecraft to reload your data pack, and press <code>F3+T</code> to reload your resource pack.</p></div></aside>',1);function l(e){var t=a();n(56),i(e,t)}export{l as component};
