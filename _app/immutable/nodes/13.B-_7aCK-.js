import"../chunks/DsnmJJEf.js";import"../chunks/DloFAEu7.js";import{n as i,o as t,q as n}from"../chunks/BTBjDpav.js";var s=t('<h1 id="variants">Variants</h1> <h2 id="what-is-a-variant">What is a Variant?</h2> <p>A <strong>Variant</strong> is a named appearance state for a Blueprint.</p> <p>Variants let one rig switch between multiple visual configurations without being re-summoned. They are evaluated <strong>per rig instance</strong>, so two copies of the same rig can use different variants at the same time.</p> <p>Common uses include:</p> <ul><li>different costumes or skins,</li> <li>damaged vs. undamaged states,</li> <li>lit vs. unlit props,</li> <li>facial expression swaps,</li> <li>hiding or revealing optional model parts.</li></ul> <p>See the <a href="/docs/function-api/variants">Function API: Variants</a> page for the runtime control function.</p> <h2 id="what-a-variant-changes">What a Variant Changes</h2> <p>A Variant can affect several parts of a rig at once.</p> <h3 id="texture-swaps">Texture swaps</h3> <p>A variant can replace one texture with another for the affected rig models.</p> <p>This is the most common use case: one rig structure, multiple looks.</p> <h3 id="excluded-nodes">Excluded Nodes</h3> <p>A variant can exclude specific nodes so they are not modified when the variant is applied.</p> <p>This is useful when layering variants on top of each other — for example, a <code>red</code> variant that recolors the whole rig, and a <code>hat</code> variant that only shows a hat without affecting the recolor.</p> <h3 id="per-variant-display-entity-config">Per-variant display entity config</h3> <p>Display entity settings can also change per variant.</p> <p>That includes settings such as:</p> <ul><li>billboard mode,</li> <li>brightness override,</li> <li>glow settings,</li> <li>shadow settings,</li> <li>variant-specific on-apply commands.</li></ul> <p>These are configured from the <a href="/docs/configs/display-entity">Display Entity Config</a> dialog.</p> <h2 id="default-variant">Default Variant</h2> <p>Every Blueprint always has a <strong>Default</strong> variant.</p> <p>The Default variant represents the rig’s base appearance. If you summon a rig without explicitly applying another variant, the rig starts in its default state.</p> <p>Think of all other variants as overrides on top of that baseline.</p> <h2 id="variants-are-per-rig-instance">Variants Are Per Rig Instance</h2> <p>Variants are not a global toggle for the blueprint.</p> <p>If you summon the same blueprint multiple times:</p> <ul><li>one rig can use the default variant,</li> <li>another can use an <code>angry</code> variant,</li> <li>another can switch variants mid-animation.</li></ul> <p>This is why variant functions are executed <strong>as the rig root entity</strong>.</p> <h2 id="how-variants-fit-into-a-rig">How Variants Fit Into a Rig</h2> <p>Variants do not create a different rig structure.</p> <p>They operate on the existing rig by changing:</p> <ul><li>which models/textures are used,</li> <li>which nodes are visible,</li> <li>which display entity settings apply.</li></ul> <p>That means variants are usually much cheaper and cleaner than creating separate blueprints for every look.</p> <h2 id="variants-and-animations">Variants and Animations</h2> <p>Variants work well with animations.</p> <p>A Variant can be:</p> <ul><li>applied manually through the variants function API,</li> <li>selected when the rig is summoned,</li> <li>triggered by a Variant Keyframe inside an animation.</li></ul> <p>This makes them useful for stateful animation work such as:</p> <ul><li>changing expression during a cutscene,</li> <li>swapping weapon forms mid-attack,</li> <li>changing prop appearance at a specific frame.</li></ul> <h2 id="when-to-use-variants-vs-separate-blueprints">When To Use Variants vs Separate Blueprints</h2> <p>Use <strong>Variants</strong> when:</p> <ul><li>the rig is fundamentally the same object,</li> <li>animations can be shared,</li> <li>only the appearance or visible parts change.</li></ul> <p>Use <strong>separate Blueprints</strong> when:</p> <ul><li>the structure is substantially different,</li> <li>the animation set is unrelated,</li> <li>export settings or rig behavior need to diverge heavily.</li></ul> <h2 id="variant-layering">Variant Layering</h2> <p>Animated Java does <strong>not</strong> maintain a formal stack of active variants.</p> <p>Applying a variant simply updates the nodes that variant affects. If you apply another variant afterward, the second variant updates its own target nodes on top of the rig’s current state.</p> <p>That means layering is:</p> <ul><li><strong>order-dependent</strong>,</li> <li><strong>partial</strong>,</li> <li><strong>not automatically reset back to Default</strong> before each apply.</li></ul> <h3 id="what-this-means-in-practice">What This Means In Practice</h3> <p>If Variant A changes one set of nodes and Variant B changes a different set of nodes, you can apply them sequentially and the result will often look like they are layered together.</p> <p>If both variants affect the same node, the later application wins for whatever it overrides on that node.</p> <p>Examples:</p> <ul><li>Two variants that swap different bones can be combined safely.</li> <li>Two variants that both replace the same bone model are effectively last-write-wins.</li> <li>Two variants that both apply display entity config to the same node are also order-dependent.</li></ul> <h3 id="safe-layering-pattern">Safe Layering Pattern</h3> <p>Variant layering works best when each variant is responsible for a separate concern, such as:</p> <ul><li>costume pieces,</li> <li>facial expressions,</li> <li>weapon state,</li> <li>glow or brightness presentation on a separate node.</li></ul> <h3 id="avoid-these-assumptions">Avoid These Assumptions</h3> <ul><li>Do not assume applying a variant clears all previous variant effects.</li> <li>Do not assume variants are merged globally into one resolved state object.</li> <li>Do not assume excluded nodes in one variant automatically restore nodes changed by a previous variant.</li></ul> <h3 id="recommended-approach">Recommended Approach</h3> <p>If you need deterministic combined looks, create an explicit combined variant such as:</p> <ul><li><code>angry_armored</code></li> <li><code>lit_damaged</code></li> <li><code>smiling_hat</code></li></ul> <p>Use sequential variant application only when you intentionally want order-based partial overrides.</p> <h2 id="related-reading">Related Reading</h2> <ul><li><a href="/docs/core-concepts/blueprints">Blueprints</a></li> <li><a href="/docs/core-concepts/rigs">Rigs</a></li> <li><a href="/docs/core-concepts/animations">Animations</a></li> <li><a href="/docs/configs/display-entity">Display Entity Config</a></li> <li><a href="/docs/function-api/variants">Function API: Variants</a></li></ul>',1);function p(a){var e=s();n(130),i(a,e)}export{p as component};
