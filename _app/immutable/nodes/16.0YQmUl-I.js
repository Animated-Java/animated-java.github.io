import"../chunks/DsnmJJEf.js";import"../chunks/BAHmlV-s.js";import{n as s,o as e,q as t}from"../chunks/CU_h1nOt.js";var o=e(`<h2 id="overview">Overview</h2> <p>The summon function spawns a new <a href="/docs/core-concepts/rigs">Rig</a> instance at the caller’s position and rotation. It creates the root entity with all bone passengers, locators, interactions, and cameras, then fires any configured on-summon event functions.</p> <h2 id="function-path">Function Path</h2> <pre class="language-null"><code>&lt;blueprint_id>/summon</code></pre> <p><strong>Example</strong> (blueprint ID <code>my_pack:my_rig</code>):</p> <pre class="language-mcfunction"><code><span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/summon</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">args</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span><span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span></code></pre> <p>The function must be called with a <code>&#123;args: &#123;&#125;&#125;</code> macro compound. You can pass optional arguments inside <code>args</code>.</p> <h2 id="arguments">Arguments</h2> <p>All arguments are optional unless stated otherwise.</p> <pre class="language-mcfunction"><code><span class="token keyword">function</span> <span class="token operator">&lt;</span><span class="token attr-name property">blueprint_id</span><span class="token operator">></span><span class="token operator">/</span><span class="token attr-name property">summon</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">args</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span>
    <span class="token attr-name property">variant</span><span class="token operator">:</span> <span class="token string">"variant_name"</span><span class="token operator">,</span>
    <span class="token attr-name property">animation</span><span class="token operator">:</span> <span class="token string">"animation_name"</span><span class="token operator">,</span>
    <span class="token attr-name property">frame</span><span class="token operator">:</span> <span class="token number">0</span><span class="token operator">,</span>
    <span class="token attr-name property">start_animation</span><span class="token operator">:</span> <span class="token boolean">false</span>
<span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span></code></pre> <ul><li><h4 id="variant-string-optional"><code>variant</code> (string, optional)</h4> <p>The <a href="/docs/core-concepts/blueprints#blueprint-id">storage name</a> of the variant to apply immediately after summoning. If omitted, the <strong>Default</strong> variant is used.</p> <p>If the variant name is invalid, the rig is automatically removed and an error is logged.</p> <p><strong>Example:</strong> <code>variant: "angry"</code> applies the <code>angry</code> variant on spawn.</p></li> <li><h4 id="animation-string-optional"><code>animation</code> (string, optional)</h4> <p>The name of the animation to seek to after summoning. The rig is placed at the specified <code>frame</code> of this animation but does <strong>not</strong> play it unless <code>start_animation</code> is also set.</p> <p>If the animation name is invalid or the frame number is negative, the rig is automatically removed and an error is logged.</p></li> <li><h4 id="frame-int-optional-default-0"><code>frame</code> (int, optional, default: <code>0</code>)</h4> <p>The animation frame to seek to. Only used when <code>animation</code> is specified. Frame indices are zero-based.</p></li> <li><h4 id="start_animation-boolean-optional-default-false"><code>start_animation</code> (boolean, optional, default: <code>false</code>)</h4> <p>When <code>true</code>, the animation specified by <code>animation</code> begins playing immediately after summoning (equivalent to calling <code>play</code> on it). When <code>false</code>, the rig is placed at the specified frame but the animation stays paused.</p></li></ul> <h2 id="examples">Examples</h2> <p>Summon with default settings:</p> <pre class="language-mcfunction"><code><span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/summon</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">args</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span><span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span></code></pre> <p>Summon at the current position with a specific variant:</p> <pre class="language-mcfunction"><code><span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/summon</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">args</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">variant</span><span class="token operator">:</span> <span class="token string">"blue"</span><span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span></code></pre> <p>Summon with a specific rotation (always use <code>rotated</code> to control facing direction):</p> <pre class="language-mcfunction"><code><span class="token keyword">execute</span> <span class="token keyword">rotated</span> <span class="token number">0</span> <span class="token number">0</span> <span class="token keyword">run</span> <span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/summon</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">args</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span><span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span></code></pre> <p>Summon and immediately start the <code>walk</code> animation:</p> <pre class="language-mcfunction"><code><span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/summon</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">args</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">animation</span><span class="token operator">:</span> <span class="token string">"walk"</span><span class="token operator">,</span> <span class="token attr-name property">start_animation</span><span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span></code></pre> <p>Summon and seek to frame 10 of the <code>idle</code> animation without playing it:</p> <pre class="language-mcfunction"><code><span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/summon</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">args</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">animation</span><span class="token operator">:</span> <span class="token string">"idle"</span><span class="token operator">,</span> <span class="token attr-name property">frame</span><span class="token operator">:</span> <span class="token number">10</span><span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span></code></pre> <h2 id="execution-context">Execution Context</h2> <p>The summon function can be called from any context. The rig is spawned <strong>at the caller’s position and rotation</strong>. Use <code>execute positioned</code> / <code>execute rotated</code> / <code>execute at</code> before calling summon to control where the rig appears.</p> <pre class="language-mcfunction"><code><span class="token comment"># Summon at a specific location facing north</span>
<span class="token keyword">execute</span> <span class="token keyword">positioned</span> <span class="token number">10</span> <span class="token number">64</span> <span class="token number">20</span> <span class="token keyword">rotated</span> <span class="token number">0</span> <span class="token number">0</span> <span class="token keyword">run</span> <span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/summon</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">args</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span><span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span>

<span class="token comment"># Summon at a player's position</span>
<span class="token keyword">execute</span> <span class="token keyword">at</span> <span class="token selector class-name">@p</span> <span class="token keyword">run</span> <span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/summon</span> <span class="token punctuation">&#123;</span><span class="token attr-name property">args</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span><span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span></code></pre> <h2 id="notes">Notes</h2> <ul><li>The summon function uses Minecraft’s <a href="https://minecraft.wiki/w/Function_(Java_Edition)#Macro" rel="nofollow">macro</a> syntax — you must always pass the <code>&#123;args: &#123;...&#125;&#125;</code> compound even if you pass no arguments.</li> <li>All per-node <code>on_summon_function</code> callbacks and the blueprint-level <code>on_summon_function</code> are called at the end of the summon sequence.</li> <li>The rig’s first tick runs on the <strong>next</strong> game tick after summon.</li></ul>`,1);function i(n){var a=o();t(52),s(n,a)}export{i as component};
