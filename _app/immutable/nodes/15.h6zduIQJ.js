import"../chunks/DsnmJJEf.js";import"../chunks/DloFAEu7.js";import{n as t,o as s,q as a}from"../chunks/BTBjDpav.js";var o=s(`<p>Animated Java generates three remove functions for every blueprint. They let you remove a single rig instance, all instances of the rig, or all entities associated with the rig.</p> <p>All remove functions (except <code>remove/entities</code>) fire any configured <code>on_remove_function</code> hooks and clean up data storage before killing entities.</p> <h2 id="removethis">remove/this</h2> <p>Removes the <strong>single rig instance</strong> you are executing as.</p> <p><strong>Function path:</strong></p> <pre class="language-null"><code>&lt;blueprint_id>/remove/this</code></pre> <p><strong>Must be run as the root entity of the rig</strong> (the entity with the <code>&lt;blueprint_id&gt;.root</code> tag).</p> <pre class="language-mcfunction"><code><span class="token comment"># Remove a specific rig by selector</span>
<span class="token keyword">execute</span> <span class="token keyword">as</span> <span class="token selector class-name">@e</span><span class="token punctuation">[</span><span class="token attr-name property">tag</span><span class="token operator">=</span><span class="token attr-name property">my_pack.my_rig.root</span><span class="token operator">,</span><span class="token attr-name property">limit</span><span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">]</span> <span class="token attr-name property">run</span> <span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/remove/this</span>

<span class="token comment"># Remove the rig the command block is riding</span>
<span class="token keyword">execute</span> <span class="token keyword">as</span> <span class="token selector class-name">@e</span><span class="token punctuation">[</span><span class="token attr-name property">tag</span><span class="token operator">=</span><span class="token attr-name property">my_pack.my_rig.root</span><span class="token operator">,</span><span class="token attr-name property">sort</span><span class="token operator">=</span><span class="token attr-name property">nearest</span><span class="token operator">,</span><span class="token attr-name property">limit</span><span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">]</span> <span class="token attr-name property">run</span> <span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/remove/this</span></code></pre> <p><strong>What it does:</strong></p> <ol><li>Runs the blueprint-level <code>on_remove_function</code>.</li> <li>Runs any per-node <code>on_remove_function</code> callbacks.</li> <li>Kills the root entity and all its passengers.</li> <li>Kills any free-floating entities (interactions, locator entities, cameras).</li> <li>Removes the rig’s data storage entry.</li></ol> <h2 id="removeall">remove/all</h2> <p>Removes <strong>every instance</strong> of this rig from the world.</p> <p><strong>Function path:</strong></p> <pre class="language-null"><code>&lt;blueprint_id>/remove/all</code></pre> <p>Can be called from any context. Equivalent to running <code>remove/this</code> as all root entities of this blueprint.</p> <pre class="language-mcfunction"><code><span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/remove/all</span></code></pre> <h2 id="removeentities">remove/entities</h2> <p>Kills <strong>every entity</strong> associated with this blueprint — including passengers, floating entities, interactions, cameras, and the root entity — without running any <code>on_remove_function</code> hooks.</p> <p><strong>Function path:</strong></p> <pre class="language-null"><code>&lt;blueprint_id>/remove/entities</code></pre> <p>This is a lower-level cleanup function. Prefer <code>remove/this</code> or <code>remove/all</code> for normal use, since those properly run lifecycle hooks and clean up storage.</p> <pre class="language-mcfunction"><code><span class="token keyword">function</span> <span class="token resource-name function">my_pack:my_rig/remove/entities</span></code></pre> <aside class="admonition admonition-warning"><p class="admonition-title">Warning</p><div class="admonition-content"><p>This removes every entity related to this blueprint simultaneously. It is primarily intended for emergency cleanup if something goes wrong with the rig and normal removal isn’t working. Use with caution.</p></div></aside> <h2 id="tags-used-for-identification">Tags Used for Identification</h2> <p>Animated Java tags every rig entity with scoreboard tags to make selection easy:</p> <table><thead><tr><th>Tag</th><th>Selects</th></tr></thead><tbody><tr><td><code>aj.global.root</code></td><td>Root entity of any AJ rig</td></tr><tr><td><code>aj.global.entity</code></td><td>Any entity belonging to any AJ rig</td></tr><tr><td><code>&lt;blueprint_id&gt;.root</code></td><td>Root entity of this specific blueprint</td></tr><tr><td><code>&lt;blueprint_id&gt;.entity</code></td><td>Any entity belonging to this blueprint</td></tr></tbody></table> <p><strong>Examples:</strong></p> <pre class="language-mcfunction"><code><span class="token comment"># Kill all AJ entities from ALL blueprints (emergency cleanup)</span>
<span class="token keyword">kill</span> <span class="token selector class-name">@e</span><span class="token punctuation">[</span><span class="token attr-name property">tag</span><span class="token operator">=</span><span class="token attr-name property">aj.global.entity</span><span class="token punctuation">]</span>

<span class="token comment"># Select this rig's root from any context</span>
<span class="token keyword">execute</span> <span class="token keyword">as</span> <span class="token selector class-name">@e</span><span class="token punctuation">[</span><span class="token attr-name property">tag</span><span class="token operator">=</span><span class="token attr-name property">my_pack.my_rig.root</span><span class="token punctuation">]</span> <span class="token attr-name property">run</span> <span class="token range">..</span><span class="token operator">.</span></code></pre>`,1);function c(e){var n=o();a(54),t(e,n)}export{c as component};
