import"../chunks/DsnmJJEf.js";import"../chunks/BAHmlV-s.js";import{n as i,o as n,q as a}from"../chunks/CU_h1nOt.js";var o=n(`<h1 id="rigs">Rigs</h1> <h2 id="what-is-a-rig">What is a Rig?</h2> <p>A <strong>Rig</strong> is a live Minecraft entity structure spawned from a Blueprint. Every time you summon a Blueprint in-game, a new <strong>Rig instance</strong> is created. A single world can contain many independent instances of the same Blueprint simultaneously.</p> <p>A Rig is made up of a hierarchy of Minecraft entities working together:</p> <pre class="language-null"><code>Root Entity (minecraft:item_display)
└── Bone Passengers (minecraft:item_display)       ← custom model cubes &amp; groups
└── TextDisplay Passengers (minecraft:text_display)
└── ItemDisplay Passengers (minecraft:item_display)
└── BlockDisplay Passengers (minecraft:block_display)
└── Interaction Entities (minecraft:interaction)   ← free-floating, nearby
└── Locator Entities (*)                           ← free-floating, if configured
└── Camera Entities (minecraft:item_display)       ← free-floating</code></pre> <h2 id="root-entity">Root Entity</h2> <p>The <strong>Root Entity</strong> is the anchor of the entire rig. It is a <code>minecraft:item_display</code> entity placed at the rig’s origin point. Every other entity in the rig either rides the root entity as a <strong>passenger</strong> or is tracked relative to it.</p> <p>The root entity carries a set of scoreboard tags identifying it:</p> <table><thead><tr><th>Tag</th><th>Meaning</th></tr></thead><tbody><tr><td><code>aj.global.root</code></td><td>This entity is a rig root</td></tr><tr><td><code>aj.global.entity</code></td><td>This entity belongs to any AJ rig</td></tr><tr><td><code>&lt;blueprint_id&gt;.root</code></td><td>Root of the specific blueprint</td></tr><tr><td><code>&lt;blueprint_id&gt;.entity</code></td><td>Any entity belonging to this blueprint</td></tr></tbody></table> <h2 id="bone-entities">Bone Entities</h2> <p>Each Group node (that contains Cubes) and each display node (Item Display, Block Display, Text Display) in your Blueprint becomes a <strong>passenger</strong> of the root entity. Passengers always move with their parent entity, so the entire rig moves as one when the root entity is teleported.</p> <p>Bone entities are <code>minecraft:item_display</code> entities with a custom item model assigned. Their position and rotation are updated each tick via the <code>transformation</code> NBT field using Minecraft’s built-in entity interpolation system.</p> <h2 id="rig-identity--storage">Rig Identity & Storage</h2> <p>Every rig instance is assigned a unique integer <strong>ID</strong> via the <code>aj.id</code> scoreboard objective. This ID is used to look up the rig’s data storage entry, which stores the UUIDs of all entities in the rig.
This allows Animated Java to efficiently track and update all entities in the rig each tick, without needing to rely on expensive entity selectors or entity NBT queries.</p> <h2 id="variants">Variants</h2> <p>A <strong>Variant</strong> is a named configuration that swaps textures and/or hides specific bones at runtime. Every rig has at least one variant — the <strong>Default</strong> variant. Additional variants can be defined in Blockbench and applied via the <a href="/docs/function-api/variants">variants function API</a>.</p> <p>When a variant is applied to a rig instance, Animated Java updates the item model/CMD on each affected bone entity to point to the variant’s model, and applies any per-variant display entity config (billboard, brightness, glow, etc.).</p> <p>See <a href="/docs/core-concepts/blueprints#variants">Variants</a> for configuration details.</p> <h2 id="animations">Animations</h2> <p>Animations are played per-rig-instance. Each rig independently tracks its own animation state.</p> <p>Multiple animations can play simultaneously on the same rig, as long as they do not affect the same bones. Each animation only updates the bones listed in its <code>Included Nodes</code> set.</p> <p>See <a href="/docs/core-concepts/animations">Animations</a> to learn more about animations.</p> <p>See the <a href="/docs/function-api/animations">Function API: Animations</a> for how to control animations in-game.</p> <h2 id="rig-lifecycle">Rig Lifecycle</h2> <ol><li><strong>Summon</strong> — <code>&lt;blueprint_id&gt;/summon</code> — Spawns the root entity with all passengers. Optionally applies a variant and seeks to an animation frame.</li> <li><strong>Tick</strong> — <code>&lt;blueprint_id&gt;/on_tick</code> — Runs every game tick for every live rig instance. Advances animation frames, updates floating entity positions, fires keyframe callbacks.</li> <li><strong>Remove</strong> — <code>&lt;blueprint_id&gt;/remove/this</code> — Kills the root and all associated entities, fires on-remove hooks, cleans up storage.</li></ol> <p>Animated Java automatically registers its tick function in <code>#animated_java:global/root/on_tick</code>, which is called every tick by AJ’s global scheduler. You do not need to manually call tick functions.</p>`,1);function c(t){var e=o();a(50),i(t,e)}export{c as component};
