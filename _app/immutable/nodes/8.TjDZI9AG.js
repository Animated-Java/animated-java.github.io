import"../chunks/DsnmJJEf.js";import"../chunks/NxOm-O3r.js";import{t as n,u as a,v as o}from"../chunks/C3L1cWEi.js";var t=a('<h1 id="animations">Animations</h1> <h2 id="what-is-an-animation">What is an Animation?</h2> <p>An <strong>Animation</strong> is a timeline that changes a rig’s pose over time.</p> <p>In Animated Java, animations do not exist globally in the world. They are evaluated <strong>per rig instance</strong>. If you summon the same Blueprint ten times, each rig can be playing different animations independently.</p> <p>Animations primarily drive:</p> <ul><li>bone transforms,</li> <li>display entity transforms,</li> <li>variant changes,</li> <li>function callbacks.</li></ul> <p>See the <a href="/docs/function-api/animations">Function API: Animations</a> page for the runtime control functions.</p> <h2 id="what-an-animation-changes">What an Animation Changes</h2> <p>At a high level, an animation can affect:</p> <ul><li>position,</li> <li>rotation,</li> <li>scale,</li> <li>effect keyframes.</li></ul> <p>Transform keyframes update the rig pose. Effect keyframes trigger extra behavior such as variant changes or custom functions.</p> <h2 id="frames-and-timing">Frames and Timing</h2> <p>Animation frames are zero-indexed and advance at <strong>20 frames per second</strong>.</p> <ul><li>1 tick = 1 frame</li> <li>20 ticks = 1 second</li></ul> <p>The total frame count is based on the animation length you set in Blockbench.</p> <p>For example:</p> <ul><li>1 second animation = 20 frames</li> <li>2.5 second animation = 50 frames</li></ul> <h2 id="loop-modes">Loop Modes</h2> <p>Each animation has a <strong>loop mode</strong> that determines its behavior when it reaches the last frame:</p> <ul><li><strong><code>once</code></strong> — Plays from frame 0 to the last frame, then stops and resets to frame 0 (the default pose).</li> <li><strong><code>hold</code></strong> — Plays from frame 0 to the last frame, then freezes on the last frame.</li> <li><strong><code>loop</code></strong> — Plays from frame 0 to the last frame, then loops back to frame 0 (with an optional <strong>Loop Delay</strong> in ticks).</li></ul> <p>Use these modes for different gameplay needs:</p> <ul><li><code>once</code> for attacks, gestures, or one-shot actions</li> <li><code>hold</code> for poses like aiming, crouching, or charging</li> <li><code>loop</code> for idle, walk, run, fly, and repeated motion</li></ul> <h2 id="loop-delay">Loop Delay</h2> <p>Loop Delay adds a pause, in ticks, before a looping animation starts over.</p> <p>This only matters for <code>loop</code> animations.</p> <p>Use it when you want movement to feel less mechanical, such as:</p> <ul><li>breathing cycles,</li> <li>blinking,</li> <li>idle fidgets.</li></ul> <h2 id="excluded-nodes">Excluded Nodes</h2> <p>Animations support an <strong>excluded nodes</strong> list. Bones in this list are skipped during that animation and receive no transform updates. This lets multiple animations play simultaneously on the same rig — one animation drives the body, another drives the arms, for example.</p> <p>This is the main tool for animation layering.</p> <p>Example:</p> <ul><li>A walk animation controls hips and legs.</li> <li>An attack animation controls torso and arms.</li> <li>Because they affect different nodes, both can run at the same time.</li></ul> <h2 id="playing-multiple-animations-at-once">Playing Multiple Animations at Once</h2> <p>Animated Java allows multiple animations to run simultaneously on the same rig instance, as long as they do not compete for the same nodes.</p> <p>If two animations both try to drive the same bone, you should expect conflicts.</p> <p>Good layering patterns include:</p> <ul><li>locomotion + upper body action</li> <li>facial animation + body animation</li> <li>additive idle motion + held pose</li></ul> <h2 id="keyframe-types">Keyframe Types</h2> <p>Animations are not limited to transform keyframes.</p> <h3 id="transform-keyframes">Transform keyframes</h3> <p>These define the pose of nodes over time.</p> <h3 id="variant-keyframes">Variant keyframes</h3> <p>Variant keyframes switch the rig to a different Variant at a specific frame.</p> <p>Use these for:</p> <ul><li>expression swaps,</li> <li>costume swaps,</li> <li>toggling model states.</li></ul> <h3 id="function-keyframes">Function keyframes</h3> <p>Function keyframes run custom MCfunction logic at a specific frame.</p> <p>Use them for:</p> <ul><li>particles,</li> <li>sounds,</li> <li>gameplay events,</li> <li>syncing scripted behavior to motion.</li></ul> <h2 id="animation-state">Animation State</h2> <p>Each live rig tracks its own animation state internally.</p> <p>That means:</p> <ul><li>one rig can be idle,</li> <li>another can be walking,</li> <li>another can be paused on frame 12,</li> <li>another can be tweening into a new pose.</li></ul> <p>This is why animation control functions must be run <strong>as the rig root entity</strong>.</p> <h2 id="tweening-and-pose-changes">Tweening and Pose Changes</h2> <p>Animated Java can smoothly transition a rig from its current pose into a target animation frame.</p> <p>This is especially useful when:</p> <ul><li>entering or exiting a pose,</li> <li>switching between movement states,</li> <li>preventing harsh snapping between animations.</li></ul> <p>See the <a href="/docs/function-api/animations">Function API: Animations</a> page for the runtime tween functions.</p> <h2 id="animation-properties">Animation Properties</h2> <p>In Blockbench, right-click an animation in the <strong>Animations</strong> panel and select <strong>Animation Properties</strong> to configure:</p> <ul><li><strong>Name</strong> — Must only contain <code>[a-zA-Z0-9_.]</code>. Used in function paths and storage keys.</li> <li><strong>Loop Mode</strong> — <code>once</code>, <code>hold</code>, or <code>loop</code>.</li> <li><strong>Loop Delay</strong> — Ticks to wait before looping. Only relevant when loop mode is <code>loop</code>.</li> <li><strong>Excluded Nodes</strong> — Bones that this animation does not affect.</li></ul> <p>For a full field-by-field breakdown, see the <a href="/docs/configs/animation">Animation Config</a> page.</p> <h2 id="practical-guidelines">Practical Guidelines</h2> <ul><li>Use Excluded Nodes aggressively to reduce the number of NBT modifications per tick.</li> <li>Prefer <code>hold</code> over custom stop logic when you need a persistent final pose.</li> <li>Use function keyframes for timing-sensitive events instead of manually checking frames in unrelated tick logic.</li> <li>Keep animation names stable once datapack code depends on them.</li></ul> <h2 id="related-reading">Related Reading</h2> <ul><li><a href="/docs/core-concepts/rigs">Rigs</a></li> <li><a href="/docs/configs/animation">Animation Config</a></li> <li><a href="/docs/function-api/animations">Function API: Animations</a></li> <li><a href="/docs/function-api/variants">Variants</a></li></ul>',1);function m(e){var i=t();o(132),n(e,i)}export{m as component};
